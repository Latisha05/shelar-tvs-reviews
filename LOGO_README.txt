SHELAR TVS — LOGO FILES
=======================

Drop your two TVS logos into this folder (c:\...\ReviewFunnel\shelar-tvs\)
using these exact filenames. They are already wired into the pages.

1) logo.png   — the main logo shown in the reviewer page badge and the
                dashboard sidebar.
                BEST CHOICE: the square TVS icon (red horse + "TVS").
                Recommended: square PNG with transparent background,
                at least 200 x 200 px.

2) favicon.png — the small browser-tab icon.
                BEST CHOICE: the same square TVS icon.
                Recommended: 64 x 64 or 128 x 128 px PNG.

Optional: if you prefer the full "THE SHELAR TVS" lockup somewhere, it is
wide, so it suits a header banner rather than the small round badge. Tell
me where you want it and I'll add a banner slot.

After replacing the files, just refresh the page (Ctrl+F5) — no rebuild
needed. The current logo.png / favicon.png are placeholders.
